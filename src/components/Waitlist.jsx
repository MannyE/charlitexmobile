import React from 'react';
import NumberInput from './auth/NumberInput';

/**
 * Waitlist page component
 * This component houses the waitlist signup functionality
 * and serves as the pricing/signup page
 */
const Waitlist = () => {
  return (
    <div className="waitlist-page">
      <NumberInput />
    </div>
  );
};

export default Waitlist;
